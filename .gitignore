# Private data — never commit
raw/
data/
runs/
models/
hf_cache/
.venv/
__pycache__/
*.pyc
*.pyo

# Trained artifacts — store on the GPU host or over Tailscale, not in git
*.safetensors
*.pt
*.bin
adapter_model.*
optimizer.*
trainer_state.json
training_args.bin

# Runtime state (re-generated)
state.json
logs/
*.log

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# Local-only worked example (the teacher's own case — not for public distribution)
WORKLOG-ja.md
pipeline/build_corpus_ochiai.py
pipeline/*_ochiai.py
pipeline/*.ochiai.yaml
pipeline/versions.yaml
pipeline/eval_prompts.yaml
